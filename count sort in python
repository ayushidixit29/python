def countingsort(num):
    n=len(num)
    res=[0]*n
    c=[0]*10
    for i in range(0,n):
        c[num[i]]=c[num[i]]+1
    for i in range(1,10):
        c[i]=c[i]+c[i-1]
    i=n-1
    while i>=0:
        res[c[num[i]]-1]=num[i]
        c[num[i]]-=1
        i=i-1
    return res
num=list(map(int,input("Enter  numbers (give space between two numbers):").split()))
print(countingsort(num))
